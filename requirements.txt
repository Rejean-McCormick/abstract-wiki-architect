# requirements.txt

# CORE QA & TESTING
# -----------------
pytest>=7.0.0            # Framework for running the automated test suites
pandas>=1.5.0            # For efficient handling of large CSV test datasets

# CODE QUALITY
# ------------
black>=23.0.0            # Auto-formatter for consistent, Wikimedia-friendly style
flake8>=6.0.0            # Linter to catch syntax / style issues early
mypy>=1.8.0              # (Optional) Static type checker for engines and constructions

# AI INTEGRATION
# -------------------------
google-generativeai>=0.3.0  # Required for AI Refiner (Gemini API)
python-dotenv>=1.0.0        # Required to load .env variables in utility scripts

# BACKEND HTTP API
# ----------------
fastapi>=0.110.0         # Web framework for the Architect HTTP API
uvicorn[standard]>=0.27.0  # ASGI server used to run the FastAPI app
sqlalchemy>=2.0.0        # ORM / DB layer
alembic>=1.12.0          # DB migrations

# GRAMMATICAL FRAMEWORK
# ---------------------
pgf==1.1                 # Python bindings for GF