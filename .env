# --- CRITICAL: Grammar Engine Configuration ---
# Set to 'False' to use the Real GF Binary.
USE_MOCK_GRAMMAR=False

# --- Persistence Paths (Fixed for WSL) ---
# Points to where the build orchestrator outputs the binary
FILESYSTEM_REPO_PATH=/mnt/c/MyCode/AbstractWiki/abstract-wiki-architect

# The name of the binary created by 'build_orchestrator.py'
# (Previously this was set to 'Wiki.pgf' which caused a mismatch)
PGF_FILE=AbstractWiki.pgf

# Path to the GF Runtime Library (RGL)
# Updated to match the relative path used by the build script
GF_LIB_PATH=/mnt/c/MyCode/AbstractWiki/abstract-wiki-architect/gf-rgl

# Storage Backend: 'filesystem' or 's3'
STORAGE_BACKEND=filesystem

# --- AI Configuration (Circuit Breaker Fix) ---
# Set to 'offline' to prevent crashes while API key is invalid.
# Change to 'gemini-1.5-pro' only when you have a working key.
ARCHITECT_AI_MODEL=offline
GOOGLE_API_KEY=your_gemini_api_key_here

# --- API Settings ---
APP_ENV=development
LOG_LEVEL=INFO
#API_SECRET=dev-secret-123

# --- Database Configuration ---
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=abstract_wiki
DATABASE_URL=postgresql://user:password@db:5432/abstract_wiki

# --- Redis Configuration ---
# 'localhost' works for WSL access to Docker ports
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0