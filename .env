# .env

# --- CRITICAL: Grammar Engine Configuration ---
# Set to 'False' to use the Real GF Binary (Wiki.pgf).
# Set to 'True' if you want to test the API without loading the heavy grammar.
USE_MOCK_GRAMMAR=False

# --- Persistence Paths (Fixed) ---
# We point this to the project root because that is where 'Wiki.pgf' is located.
# (Based on your pathsall.txt file)
FILESYSTEM_REPO_PATH=/mnt/c/MyCode/AbstractWiki/abstract-wiki-architect/gf

# Path to the GF Runtime Library (RGL)
GF_LIB_PATH=/mnt/c/MyCode/AbstractWiki/gf-rgl

# Storage Backend: 'filesystem' or 's3'
STORAGE_BACKEND=filesystem

# --- Google Gemini Configuration ---
# Required for the AI Refiner (Tier 2 Upgrade System)
GOOGLE_API_KEY=your_gemini_api_key_here

# --- API Settings ---
APP_ENV=development
LOG_LEVEL=INFO
# Security key for API access (Change this in production!)
API_SECRET=dev-secret-123

# --- Database Configuration ---
# (Default for Docker setup)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=abstract_wiki
DATABASE_URL=postgresql://user:password@db:5432/abstract_wiki

# --- Redis Configuration (Messaging) ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0